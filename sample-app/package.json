{
  "name": "capgo-sample-app",
  "version": "1.0.1",
  "private": true,
  "description": "Sample Capacitor app with Capgo OTA updates",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "zip": "cd dist && zip -r ../bundle.zip . && cd .."
  },
  "dependencies": {
    "@capacitor/app": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/splash-screen": "^6.0.0",
    "@capgo/capacitor-updater": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0",
    "typescript": "^5.5.0",
    "vite": "^5.4.0"
  }
}
